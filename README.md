# dotfiles

You are best to ignore this git for now it's my first test and nothing has really been riced yet.

git pull = get from website

git commit -am "text" = add to the to be upload

git add = add new file to git

git push = upload
